# Standalone Example: Полная кластеризация с Ollama

## Описание

`examples/standalone_ollama_example.py` - это полноценный автономный пример использования библиотеки llm_clustering.

### Что демонстрирует пример:

1. ✅ **Использование Ollama** в качестве LLM-провайдера
2. ✅ **Кластеризацию 1000 обращений** из демо-данных
3. ✅ **Кастомный промпт** через business_context
4. ✅ **Итеративную обработку** с отображением прогресса
5. ✅ **Сохранение результатов** (кластеры, назначения, отчет)

## Требования

Перед запуском убедитесь что:

1. **Ollama запущена:**
   ```bash
   ollama serve
   ```

2. **Модель установлена:**
   ```bash
   ollama pull qwen3:30b
   # или другая модель, указанная в .env
   ```

3. **Библиотека установлена:**
   ```bash
   pip install -e .
   ```

4. **Демо-данные распакованы:**
   ```bash
   # Проверить наличие файла
   ls -lh ai_data/demo_sample.csv
   
   # Если файла нет, распаковать
   unzip ai_data/demo_sample.csv.zip -d ai_data/
   ```

## Быстрый запуск

### Вариант 1: Прямой запуск (рекомендуется)

```bash
python examples/standalone_ollama_example.py
```

### Вариант 2: Как исполняемый скрипт

```bash
./examples/standalone_ollama_example.py
```

## Что произойдет при запуске

### Шаг 1: Инициализация Ollama
- Подключение к Ollama API (по умолчанию http://localhost:11434/api)
- Проверка доступности модели
- Тестовый запрос для проверки работоспособности

### Шаг 2: Загрузка данных
- Чтение 1000 обращений из `ai_data/demo_sample.csv`
- Отображение примеров данных

### Шаг 3: Создание кастомного промпта
- Генерация бизнес-контекста для телеком-оператора
- Определение критериев кластеризации:
  - Простые запросы (FAQ, информационные)
  - Технические проблемы (диагностика)
  - Управление аккаунтом (тарифы, биллинг)
  - Критические ситуации (жалобы, споры)

### Шаг 4: Настройка pipeline
- Конфигурация размера батчей (50 обращений)
- Ограничение на количество кластеров за батч (10)
- Установка температуры для LLM (0.1 для стабильности)

### Шаг 5: Кластеризация
- Итеративная обработка по 50 обращений
- Отображение прогресса для каждого батча
- Обнаружение и создание новых кластеров

### Шаг 6: Анализ результатов
- Вывод итоговой статистики
- ТОП-10 кластеров по количеству обращений
- Описание каждого кластера

### Шаг 7: Сохранение результатов
Результаты сохраняются в `ai_data/standalone_example_results/`:
- `clusters_YYYYMMDD_HHMMSS.json` - JSON с кластерами
- `assignments_YYYYMMDD_HHMMSS.csv` - CSV с назначениями
- `report_YYYYMMDD_HHMMSS.txt` - Текстовый отчет

## Ожидаемое время выполнения

- **На локальной Ollama (qwen3:30b)**: 5-15 минут (в зависимости от железа)
- **На API сервисах**: 2-5 минут

## Пример вывода

```
================================================================================
STANDALONE ПРИМЕР: Кластеризация с Ollama
================================================================================

Шаг 1: Инициализация Ollama провайдера
--------------------------------------------------------------------------------
✓ Ollama провайдер инициализирован
  - API URL: http://localhost:11434/api
  - Model: qwen3:30b
  - Temperature: 0.7
✓ Ollama отвечает: OK

Шаг 2: Загрузка демо-данных
--------------------------------------------------------------------------------
Загрузка данных из ai_data/demo_sample.csv...
✓ Загружено 1000 обращений

...

Шаг 5: Запуск кластеризации
--------------------------------------------------------------------------------
Обрабатываем 1000 обращений...
(Это может занять несколько минут)

Батч 1:
  ├─ Обработано: 50/1000 (5.0%)
  ├─ Новых кластеров: 8
  └─ Всего кластеров: 8
      • Проблемы с подключением к интернету
      • Вопросы по тарифам и смене плана
      • Восстановление пароля

...

📊 ИТОГОВАЯ СТАТИСТИКА:
  • Всего обращений: 1000
  • Найдено кластеров: 42
  • Среднее обращений на кластер: 23.8

🏆 ТОП-10 КЛАСТЕРОВ ПО КОЛИЧЕСТВУ ОБРАЩЕНИЙ:
  1. Проблемы с подключением к интернету
     • Обращений: 87
     • Описание: Клиенты сообщают о проблемах с подключением...

...

✅ ПРИМЕР УСПЕШНО ЗАВЕРШЕН!
```

## Настройка под свои нужды

### Изменение количества обращений

В функции `load_demo_data()`:
```python
df = load_demo_data(limit=500)  # Вместо 1000
```

### Изменение размера батча

В функции `main()`:
```python
settings = Settings(
    clustering_batch_size=30,  # Вместо 50
    max_clusters_per_batch=5,  # Вместо 10
)
```

### Изменение промпта

Отредактируйте функцию `create_custom_business_context()`:
```python
def create_custom_business_context() -> str:
    return """
    Ваш кастомный бизнес-контекст здесь...
    """
```

### Использование другой модели

В `.env` файле:
```bash
OLLAMA_MODEL=llama3:70b
# или любая другая модель из ollama list
```

### Использование своих данных

Замените `load_demo_data()` на свою функцию:
```python
def load_my_data() -> pd.DataFrame:
    df = pd.read_csv("путь/к/вашим/данным.csv")
    # Убедитесь что есть столбец 'text'
    return df
```

## Устранение проблем

### Ошибка: "Cannot connect to Ollama"

**Решение:**
```bash
# Проверьте, что Ollama запущена
ps aux | grep ollama

# Если не запущена, запустите
ollama serve

# В другом терминале проверьте доступность
curl http://localhost:11434/api/tags
```

### Ошибка: "Демо-файл не найден"

**Решение:**
```bash
# Распакуйте демо-данные
cd /home/alex/tradeML/llm_clustering
unzip ai_data/demo_sample.csv.zip -d ai_data/
```

### Ошибка: "No module named 'llm_clustering'"

**Решение:**
```bash
# Установите библиотеку в editable mode
pip install -e .

# Или добавьте в PYTHONPATH
export PYTHONPATH=/home/alex/tradeML/llm_clustering/src:$PYTHONPATH
```

### Слишком долго выполняется

**Решение:**
- Уменьшите количество обращений: `load_demo_data(limit=100)`
- Увеличьте размер батча: `clustering_batch_size=100`
- Используйте более быструю модель: `OLLAMA_MODEL=gemma3:latest`

## См. также

- [README.md](../README.md) - Основная документация
- [quickstart.md](quickstart.md) - Руководство по быстрому старту
- [library_usage.md](library_usage.md) - Использование как библиотеки
- [examples.py](../examples.py) - Все примеры использования API

